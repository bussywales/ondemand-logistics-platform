{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @shipwright/contracts build && pnpm --filter @shipwright/observability build && pnpm --filter @shipwright/payments build && pnpm --filter worker build && tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "start:prod": "node dist/main.js",
    "start:dev": "tsx watch src/main.ts",
    "smoke:staging": "tsx ./scripts/smoke-staging.ts",
    "verify:staging": "tsx ./scripts/verify-staging.ts",
    "proof:staging-paid-delivery": "tsx ./scripts/proof-staging-paid-delivery.ts",
    "lint": "echo 'api lint placeholder'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @shipwright/contracts build && pnpm --filter @shipwright/observability build && pnpm --filter @shipwright/payments build && vitest run",
    "test:integration": "vitest run"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.11",
    "@nestjs/core": "^11.0.11",
    "@nestjs/platform-express": "^11.0.11",
    "@shipwright/contracts": "workspace:*",
    "@shipwright/observability": "workspace:*",
    "@shipwright/payments": "workspace:*",
    "worker": "workspace:*",
    "express": "^5.1.0",
    "jose": "^5.9.6",
    "pino": "^9.14.0",
    "pg": "^8.13.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.5"
  }
}
