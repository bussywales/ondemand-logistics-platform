# Staging paid-delivery proof harness. Do not commit real values.
# This file is auto-loaded by `pnpm proof:staging-paid-delivery` when present.

# Optional route/payload defaults. Safe to keep as defaults.
STAGING_PROOF_API_BASE_URL=https://api-staging-qvmv.onrender.com
STAGING_PROOF_RESTAURANT_SLUG=pilot-kitchen-1777370757
STAGING_PROOF_PAYMENT_METHOD_ID=pm_card_visa

# Required for fixture auth/session creation and direct staging record verification.
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=replace_me
DATABASE_URL=postgresql://postgres:<password>@<host>:5432/postgres

# Optional but recommended for repeatable fixture creation.
# Required when Supabase email signup is rate-limited.
SUPABASE_SERVICE_ROLE_KEY=replace_me_optional

# Optional. Default false relies on the deployed staging worker to process outbox messages.
# Set true only from a secure local shell with any provider env needed by worker side effects.
STAGING_PROOF_PROCESS_OUTBOX=false

# Required only when STAGING_PROOF_PROCESS_OUTBOX=true.
# STRIPE_SECRET_KEY=sk_test_replace_me
